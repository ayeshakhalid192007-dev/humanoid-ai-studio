// This component is being deprecated in favor of the main Button component
// which follows the unified design system. This file is kept for backward compatibility
// but will import and re-export the main Button component.
import { Button, ButtonProps } from './Button';

export { Button, type ButtonProps };