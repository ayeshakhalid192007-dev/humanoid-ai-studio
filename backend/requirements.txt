# ============================================================
# Python >= 3.10 required (Pydantic V2, asyncpg 0.29+, async patterns)
# ============================================================

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# LLM — native Google GenAI SDK (direct Gemini API, no shim needed)
google-genai>=1.0.0

# Keep openai for AIClientFactory embedding fallback
openai>=1.0.0

# Local Embeddings — sentence-transformers (free, no API key, runs locally)
# Model all-MiniLM-L6-v2 downloads ~80MB from HuggingFace on first use
sentence-transformers>=3.0.0

# Vector Database
qdrant-client>=1.7.0

# Relational Database
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Data Validation
pydantic>=2.0.0

# Caching
redis>=5.0.0

# Monitoring and Observability
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-propagator-b3>=1.24.0
prometheus-client>=0.20.0
pydantic-settings>=2.0.0

# Environment & Configuration
python-dotenv>=1.0.0

# Logging
python-json-logger>=2.0.7

# HTTP Client
httpx>=0.25.0

# Security & Authentication
python-jose[cryptography]>=3.3.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Code Quality
black>=23.0.0
flake8>=6.1.0
mypy>=1.7.0
isort>=5.12.0
